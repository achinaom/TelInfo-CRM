import { ShowTypePhonePipe } from './show-type-phone.pipe';

describe('ShowTypePhonePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowTypePhonePipe();
    expect(pipe).toBeTruthy();
  });
});
