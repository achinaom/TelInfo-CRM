import { ShowStatusPhonePipe } from './show-status-phone.pipe';

describe('ShowStatusPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStatusPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
