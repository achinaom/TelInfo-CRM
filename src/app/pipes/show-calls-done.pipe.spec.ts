import { ShowCallsDonePipe } from './show-calls-done.pipe';

describe('ShowCallsDonePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCallsDonePipe();
    expect(pipe).toBeTruthy();
  });
});
